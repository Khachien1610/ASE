<div class="container mt-4 mb-5">
    <form action="" method="POST">
        <div class="row">
            <div class="col-6 border-right">
                <h2>Thông tin giao hàng</h2>
                <div class="form-group">
                    <label for="name" class="text-muted">Họ tên khách hàng</label>
                    <input type="text" name="name" value="{{ customer.name }}" class="form-control col-9" id="name">
                </div>
                <div class="form-group">
                    <label for="email" class="text-muted">Email</label>
                    <input type="email" name="email" value="{{ customer.email }}" class="form-control col-9" id="email">
                </div>
                <div class="form-group">
                    <label for="phone" class="text-muted">Số điện thoại</label>
                    <input type="text" name="phone" value="{{ customer.phone }}" class="form-control col-9" id="phone">
                </div>
                <div class="form-group">
                    <label for="address" class="text-muted">Địa chỉ</label>
                    <input type="text" name="address" value="{{ customer.address }}" class="form-control col-9"
                        id="address">
                </div>
                <div class="custom-control custom-radio mt-4">
                    <input type="radio" id="payment1" name="payment" class="custom-control-input" value="cod" checked>
                    <label class="custom-control-label" for="payment1">Thanh toán khi nhận hàng</label>
                </div>
                <div class="custom-control custom-radio mt-2">
                    <input type="radio" id="payment2" name="payment" class="custom-control-input" value="online">
                    <label class="custom-control-label" for="payment2">Thanh toán online</label>
                </div>
            </div>
            <div class="col-6">
                <h2>Sản phẩm</h2>
                <table class="table table-hover border text-center mb-4">
                    <thead>
                        <tr>
                            <th>STT</th>
                            <th>Tên sản phẩm</th>
                            <th>Số lượng</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each cartSuccess}}
                        <tr>
                            <th>{{sum @index 1}}</th>
                            <td>
                                <a href="/products/{{ this.slug }}">
                                    {{ this.name }}
                                </a>
                            </td>
                            <td>{{ this.count }}</td>
                        </tr>
                        {{else}}
                        <tr>
                            <td colspan="7" class="text-center">
                                Không có sản phẩm nào trong giỏ hàng.
                                <a href="/"> Quay lại cửa hàng để tiếp tục mua sắm.</a>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                <div class="d-flex justify-content-between pt-3 border-top">
                    <p>Tổng số tiền tạm tính</p>
                    <p>{{ this.sum }} VNĐ</p>
                </div>
                <div class="d-flex justify-content-between align-items-center pt-3">
                    <p class="mr-auto">Điểm tích lũy</p>
                    <div class="custom-control custom-checkbox custom-point">
                        <input type="checkbox" class="custom-control-input" id="customCheck1" name="check" value="checked">
                        <label class="custom-control-label" for="customCheck1">
                            <input type="text" class="form-control" value="{{ customer.point }}" name="point">
                        </label>
                    </div>
                </div>
                <p class="text-right text-muted font-italic mb-0 mt-1">(1 điểm tích lũy tương ứng với 1000 VNĐ)</p>
                {{#if error}}
                <p class="m-0 text-danger text-right">{{ this.error }}</p>
                {{/if}}
                <div class="d-flex justify-content-between pt-3">
                    <p>Phí vận chuyển</p>
                    <p>0 VNĐ</p>
                </div>
                <div class="d-flex justify-content-between pt-4 border-top">
                    <h5>Tổng cộng</h5>
                    <h4>{{ this.sum }} VNĐ</h4>
                </div>
                <button type="submit" class="btn btn-success w-100 mt-3">THANH TOÁN</button>
            </div>
        </div>
    </form>
</div>

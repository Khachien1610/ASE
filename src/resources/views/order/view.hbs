<div class="mt-4 mb-5 container min-height">
    <h1 class="text-center mb-4">Chi tiết đơn hàng</h1>
    <table class="table table-hover border text-center">
        <thead>
            <tr>
                <th>STT</th>
                <th>Tên sản phẩm</th>
                <th>Đơn giá</th>
                <th>Giảm giá</th>
                <th>Số lượng</th>
                <th>Thành tiền</th>
            </tr>
        </thead>
        <tbody>
            {{#each orderSuccess}}
            <tr>
                <th>{{sum @index 1}}</th>
                <td>{{ this.name }}</td>
                <td>{{ this.cost }} VNĐ</td>
                <td>{{ this.sale }} %</td>
                <td>{{ this.count }}</td>
                <td>{{ this.afterSale }} VNĐ</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{#if sum}}
    <h5 class="text-right">
        <span class="text-muted">Tổng tiền:</span>
        <span class="ml-3 text-danger">{{ this.sum }} VNĐ</span>
    </h5>
    {{/if}}

</div>